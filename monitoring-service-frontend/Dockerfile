# Используйте Node.js в качестве базового образа
FROM node:14

# Установите рабочую директорию внутри контейнера
WORKDIR /app

# Копируйте файлы package.json и package-lock.json в контейнер
COPY package*.json ./

# Установите зависимости
RUN npm install

# Копируйте все файлы проекта в контейнер
COPY . .

# Откройте порт, на котором будет работать ваше приложение
EXPOSE 8081

# Запустите ваше приложение
CMD ["npm", "run", "serve"]